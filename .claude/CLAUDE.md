# Quy tắc bắt buộc cho AI trong dự án này

1. Trước khi viết bất kỳ chức năng liên quan đến database/CRUD hoặc endpoint, PHẢI đọc ĐỒNG THỜI cả 2 skill: `.claude/skills/fast-api-mysql-auto-schema/SKILL.md` (sinh code 3 tầng) và `.claude/skills/contract-first-handshake/SKILL.md` (quy trình 5 bước bàn giao Contract-First).
2. PHẢI nêu rõ: "Đang áp dụng skill nào" và "phần nào của code bị ảnh hưởng bởi skill đó" trước khi đưa ra code.
3. Không tự đoán endpoint, method, tên bảng nếu chưa có trong tài liệu hoặc chưa hỏi lại người dùng.
4. Đọc dữ liệu từ file .env, tuyệt đối không hardcode mật khẩu hay thông tin nhạy cảm.
5. Luôn tuân thủ cấu trúc thư mục chuẩn trong `backend/app/`: routes, core, models, schemas, services, repositories.
6. Sau khi thêm/sửa/xoá bất kỳ endpoint hoặc schema nào, BẮT BUỘC (theo đúng thứ tự) chạy `python scripts/export_openapi.py` để xuất lại `openapi.yaml`, rồi ghi biên bản bàn giao vào `openapi.CHANGELOG.md` (phiên bản khớp `API_VERSION` trong `app/main.py`). Chỉ được chuyển sang code Frontend (`frontend/src/types/`, `frontend/src/api/`, UI) SAU KHI đã hoàn tất 2 việc này.
```[cite: 1]

---

Sau khi lưu xong, bạn mở lại cửa sổ **VS Code**, chúng ta sẽ bắt đầu gửi lệnh cho AI Agent để sinh code cho tính năng đầu tiên[cite: 1]!

---

## Bối cảnh nghiệp vụ: Cổng thông tin nội bộ Lữ đoàn Thông tin 21 – Bộ đội Biên phòng

Dự án này là cổng thông tin nội bộ vừa phục vụ tuyên truyền chính trị/hoạt động đơn vị, vừa là kênh triển khai nhiệm vụ có kiểm soát truy cập. Các quy ước dưới đây là **rule cố định** — khi người dùng chỉ mô tả ngắn gọn một tính năng thuộc các mục dưới, áp dụng đúng role/quyền tương ứng mà không cần hỏi lại.

### Quy ước giá trị `role` (trường `role` trong `User`)
| role | Đối tượng thực tế | Quyền mặc định |
|---|---|---|
| `admin` | Tài khoản quản trị hệ thống (chủ đơn vị giữ) | Kế thừa **toàn bộ** quyền `commander` + độc quyền: cấp cờ 2 kênh hạn chế (`PATCH /users/{id}/channel-access`), cấp lại mật khẩu (`POST /users/{id}/reset-password`), CRUD đơn vị (`/units`), tạo tài khoản `admin` khác. Seed sẵn `admin`/`admin` (đổi trong `.env` `SYSTEM_ADMIN_*`), cờ `is_system` chống khoá/hạ quyền/đổi (409), **miễn** quy tắc mật khẩu. `require_roles("commander")` tự động chấp nhận `admin`. |
| `commander` | Lữ trưởng, Chính ủy, Phó Lữ trưởng, Phó Chính ủy | Toàn quyền: ban hành Chỉ thị-Nhiệm vụ, duyệt/đăng mọi nội dung, quản lý tài khoản người dùng |
| `officer` | Cán bộ, sĩ quan/QNCN các phòng ban, đại đội | Đăng/biên tập Tin tức–Hoạt động và Giáo dục chính trị; chỉ xem Chỉ thị-Nhiệm vụ; không quản lý tài khoản |
| `soldier` (mặc định) | Chiến sĩ | Chỉ xem Tin tức, Giáo dục chính trị, Chỉ thị-Nhiệm vụ liên quan; không đăng/sửa |

**Đăng ký + bậc truy cập (đã triển khai):**
- Tự đăng ký: `POST /users/register` (công khai) → tạo `role="soldier"`, `is_active=False`. Không đăng nhập được (login → 403) cho tới khi `commander` gọi `POST /users/{id}/activate`. `POST /users/{id}/deactivate` để khoá lại. `GET /users/?active=false` để xem danh sách chờ.
- Cấp quyền đăng bài = đổi role: `PATCH /users/{id}/role` body `{role: "soldier"|"officer"|"commander"}` (chỉ `commander`). `officer` = được đăng/sửa Tin tức + GDCT. Chặn: không tự hạ quyền / tự khoá tài khoản chính mình (400); luôn giữ ≥ 1 `commander` đang `is_active` (409) — áp cả cho `deactivate` và `role`.
- Tạo/sửa nội dung phân loại `mat` (posts, documents) yêu cầu `has_secret_clearance(current_user)` (`commander` hoặc `clearance=True`) → nếu không: 403. FE ẩn lựa chọn "MẬT" trong form với user không đủ quyền.
- 3 bậc phân loại nội dung (`classification` trên `posts`, `directives`, `documents`): `cong_khai` (khách xem được) · `noi_bo` (mọi tài khoản đã kích hoạt) · `mat` (chỉ `commander` HOẶC user có `User.clearance = True`). Commander cấp/thu quyền: `PATCH /users/{id}/clearance` body `{clearance: bool}`. Helper: `app/core/access.py` (`allowed_classifications`, `can_view_classification`, `has_secret_clearance`).
- JWT thêm claim `clr` (bool clearance). FE `AuthContext` đọc thành `hasClearance` (ẩn/hiện lựa chọn bậc "MẬT" + chip cảnh báo) — không thay cho enforce ở backend.

### Sitemap & phân quyền theo mục
| Mục | Xem | Đăng/Sửa | Trạng thái | Ghi chú |
|---|---|---|---|---|
| Trang chủ | tất cả (đã đăng nhập) | — | **Đã triển khai** (backend) | `GET /home/summary?limit=` gộp `latest_posts`, `latest_education_materials`, `latest_directives`, `latest_announcements`; xem `home.py`, `home_service.py`. Banner/UI là việc frontend |
| Tin tức – Hoạt động đơn vị | **công khai** bài `da_duyet` (không cần JWT); `officer` thấy thêm bài của mình mọi trạng thái; `commander` thấy tất cả | `officer`, `commander` (sửa/xoá bài người khác chỉ `commander`) | **Đã triển khai** | Entity `posts`; danh mục `huan_luyen`, `dan_van`, `khen_thuong`, `guong_nguoi_tot`. **Luồng duyệt**: `officer` đăng → `cho_duyet`; `commander` đăng → `da_duyet` luôn; `POST /posts/{id}/review` (chỉ `commander`, body `{status: da_duyet\|tra_lai, review_note?}`); `officer` sửa bài đã duyệt/trả lại → tự về `cho_duyet`. Upload ảnh: `POST /posts/{id}/thumbnail` (multipart `file`, ảnh) → set `cover_image_url` = `/static/posts/...`. `PostOut` kèm `status`, `review_note`, `reviewed_by_id`, `reviewed_at`. Xem `PostsPage.tsx`, `posts.py` |
| Thông báo nội bộ & Lịch trực kíp | Thông báo: **công khai** khi `is_public`, còn lại cần JWT. Lịch trực: cần JWT | `officer`, `commander` (sửa/xoá của người khác chỉ `commander`) | **Đã triển khai** | Entity `announcements` (`priority`: `thap`/`binh_thuong`/`cao`/`khan`, `is_pinned`, `is_public`, `starts_at`/`ends_at`; sắp xếp ghim→ưu tiên→mới nhất) và `duty_schedules` (`duty_date`, `shift`, `duty_officer`, `role_title`, `note`; lọc `date_from`/`date_to`). Xem `announcements.py`, `duty_schedules.py` |
| Văn bản – Tài liệu – Biểu mẫu | **công khai** khi `is_public`, còn lại cần JWT | `officer`, `commander` (sửa/xoá của người khác chỉ `commander`) | **Đã triển khai** | Entity `documents`; `category` cố định: `bieu_mau`, `huong_dan`, `quy_che_quy_dinh`, `ke_hoach`, `bao_cao`, `van_ban_chi_dao`. Upload multipart `POST /documents` (`title`, `category`, `description?`, `is_public`, `file` — .pdf/.doc/.docx/.xls/.xlsx/.ppt/.pptx, ≤ `MAX_UPLOAD_MB`). Tải về: `GET /documents/{id}/download` (FileResponse, tôn trọng `is_public`). File lưu ở `storage/uploads/documents/`, phục vụ qua `/static`. Xem `documents.py` |
| Giáo dục chính trị | tất cả (đã đăng nhập) | `officer`, `commander` (sửa/xoá bài người khác chỉ `commander`) | **Đã triển khai** | Entity `education_materials`; danh mục `hoc_tap_chinh_tri_quan_su`, `tuyen_truyen`, `phap_luat_bien_gioi`, `lich_su_truyen_thong`; có `period_label` (VD "Tuần 35/2026") cho nội dung định kỳ tuần/tháng, `attachment_url` cho tài liệu đính kèm; xem `EducationPage.tsx`, `education_materials.py` |
| Chỉ thị – Nhiệm vụ | tất cả (đã đăng nhập) thấy chỉ thị `da_ban_hanh`; `commander` thấy cả bản `nhap` | chỉ `commander` | **Đã triển khai** | Entity `directives` (`status`: `nhap`/`da_ban_hanh`) + bảng `directive_acknowledgements` (mỗi user bấm `POST /directives/{id}/acknowledge` để "đã tiếp thu", idempotent). `GET /directives/{id}/acknowledgements` (chỉ `commander`) trả `acknowledged`/`pending` theo danh sách user active để theo dõi mức độ quán triệt; `DirectiveOut` kèm `recipient_count`, `acknowledged_count`, `acknowledged_by_me`. Xem `directives.py`, `directive_service.py` |
| Kênh Chỉ đạo – Báo cáo (luồng trao đổi) | tài khoản có `directive_channel_access` (hoặc `commander`/`admin`) | như cột Xem | **Đã triển khai (Phase 2)** | Entity `directive_threads` + `directive_messages` + `directive_thread_reads`. BCH/`commander`/`admin` (và tài khoản có cờ kênh thuộc đơn vị `bch_lu_doan`) thấy mọi luồng; tài khoản đơn vị cấp dưới chỉ thấy luồng đơn vị mình (ngoài phạm vi → 404). `POST /directive-threads` (BCH chọn `unit_id`, tài khoản đơn vị bị ép về đơn vị mình), `POST /directive-threads/{id}/messages` (multipart `body` + `file?` → `/static/directive/…`; 409 nếu đã đóng), `PATCH /directive-threads/{id}/close` (chỉ `commander`/`admin`). Theo dõi đã đọc qua `last_read_message_id`. Xem `directive_threads.py`, `directive_thread_service.py`, helper `directive_thread_scope` ở `app/core/access.py` |
| Kênh Chỉ đạo – Báo cáo (giao nhiệm vụ) | tài khoản có `directive_channel_access` (hoặc `commander`/`admin`) | tạo/sửa/huỷ/duyệt: **chỉ `commander`/`admin`**; nộp báo cáo: tài khoản thuộc `unit_id`/`assignee_id` của target | **Đã triển khai (Phase 3)** | Entity `directive_assignments` (FK `directives` nullable, `status`: `chua_giao`/`dang_thuc_hien`/`hoan_thanh`; `qua_han` suy diễn từ `due_date`) + `directive_assignment_targets` (`unit_id`, `assignee_id?`, `status`: `chua_nop`/`cho_duyet`/`da_duyet`/`tra_lai`) + `directive_submissions` (lịch sử nộp, `content` + `attachment_url` `/static/documents/…`, `review_result`/`review_note`/`reviewed_*`). `POST /directive-assignments/{id}/targets/{tid}/submit` (multipart `content`+`file?`; 409 nếu đã `da_duyet`), `.../review` (`{result, review_note?}`; 409 nếu không ở `cho_duyet`). Trạng thái nhiệm vụ tự tính lại. Xem `directive_assignments.py`, `directive_assignment_service.py` |
| Kênh chuyên BCH & Cấp uỷ | **`has_secret_clearance`**: `commander`/`admin` HOẶC `User.clearance = True` (không đủ → 403) | luồng trao đổi: mọi thành viên đủ quyền; **vào sổ / sửa / xoá công văn, đóng luồng: chỉ `commander`/`admin`** | **Đã triển khai (Phase 4)** | `command_threads` + `command_messages` + `command_thread_reads` (`classification="mat"`, không phân đơn vị): `POST /command-threads/{id}/messages` (multipart `body`+`file?` → `/static/command/…`, 409 nếu đóng), `PATCH .../close`. `official_dispatches` (sổ công văn: `direction` `di`/`den`, `dispatch_number` unique theo chiều, `summary`, `issuing_org`/`receiving_org`, `issued_date`/`received_date`, `status` `moi`/`dang_xu_ly`/`da_xu_ly`/`luu_tru`, `attachment_url`) + `dispatch_acknowledgements` (sổ ký nhận: `acknowledged_at`, `response_note`, upsert). `POST/PUT /official-dispatches` multipart; `POST /{id}/acknowledge`; `GET /{id}/download` (FileResponse có kiểm soát quyền, không qua `/static`). Helper `can_access_command_channel` ở `app/core/access.py`. Xem `command_threads.py`, `official_dispatches.py` |
| Kênh chuyên — Giao ban trực tuyến | **`has_secret_clearance`** (không đủ → 403) | tạo/sửa/huỷ/ghi biên bản/điểm danh người khác/mời-gỡ thành phần: **chỉ `commander`/`admin`**; thành viên được triệu tập tự cập nhật điểm danh / lý do vắng / ý kiến của **chính mình** | **Đã triển khai (Phase 5)** | `command_meetings` (`start_time`/`end_time`, `location`, `meeting_link` — link phòng họp ngoài, KHÔNG dựng video; `agenda`, `minutes`, `attachment_url`, `status` `sap_dien_ra`/`dang_dien_ra`/`da_ket_thuc`/`da_huy`, `classification="mat"`) + `command_meeting_attendees` (`user_id`, `unit_id`, `attendance` `co_mat`/`vang_mat`/`chua_diem_danh`, `absence_reason`, `contribution_note`, unique `meeting_id+user_id`). `POST /command-meetings/{id}/minutes` (`{minutes, mark_finished?}`), `.../attachment` (multipart), `.../download` (FileResponse có kiểm soát quyền), `.../attendees` (mời, 409 nếu trùng), `PATCH .../attendees/{user_id}` (chỉ huy: bất kỳ; thành viên: chính mình → 403 nếu khác). `end_time < start_time` → 400. Xem `command_meetings.py`, `command_meeting_service.py` |
| Quản lý người dùng | `commander` / `admin` | `commander` / `admin` | **Đã triển khai** | `UsersPage.tsx`, `users.py` |
| Hồ sơ cá nhân | chủ tài khoản | chủ tài khoản | **Đã triển khai** | `GET /profile/me`, `PUT /profile/me` (sửa `full_name`), `POST /profile/change-password` (`current_password` + `new_password`, sai mật khẩu cũ → 400, thành công → 204). Xem `profile.py`, dùng lại `user_service` |

### RBAC (đã triển khai — dùng làm khuôn mẫu cho các module Chưa làm ở trên)
- `app/api/deps.py`: `get_current_user` (bắt buộc JWT hợp lệ), `get_optional_user` (trả `User | None`, KHÔNG raise 401 — dùng cho endpoint công khai có phân biệt khách/đã đăng nhập, ví dụ `GET /posts`, `/announcements`, `/documents`), `require_roles(*roles)` (chặn 403 nếu role không khớp), `bootstrap_or_role(*roles)` (cho phép thao tác không cần đăng nhập CHỈ KHI bảng `users` đang rỗng — dùng cho tạo tài khoản `commander` đầu tiên).
- Public/Guest (mạng LAN): chỉ các endpoint **GET** của Tin tức (`da_duyet` + `classification=cong_khai`), Thông báo (`is_public`), Tài liệu (`classification=cong_khai`) và `GET /home/public` là mở; mọi thao tác ghi và module khác vẫn cần JWT. Map role: `commander` = Admin/Ban chỉ huy, `officer` = Cán bộ/Nhân viên, `soldier` = nội bộ chỉ xem.
- Bậc phân loại phải lọc ở **service** qua `allowed_classifications(current_user)` (danh sách filter) và `can_view_classification(item.classification, user)` (kiểm tra 1 item → 404 nếu không được xem, không lộ tồn tại). Xem `post_service`, `directive_service`, `document_service` làm mẫu.
- Upload file: `app/core/uploads.py` (`save_upload`/`delete_upload`), lưu `storage/uploads/<subdir>/<uuid>.<ext>`, phục vụ qua `app.mount("/static", StaticFiles(...))`. Cấu hình `UPLOAD_DIR`, `MAX_UPLOAD_MB`, `EXTRA_CORS_ORIGINS` trong `.env` (đều có default). CORS đã mở regex cho `localhost`/`127.0.0.1` mọi cổng + dải LAN `10.x`, `192.168.x`, `172.16-31.x`.
- Migration thủ công (bảng cũ, `create_all` không thêm cột): chạy 1 lần theo thứ tự — `scripts/migrate_posts_approval.py` (posts: 4 cột duyệt bài) → `scripts/migrate_access_tiers.py` (users.clearance, posts.classification+is_featured, directives.classification, documents.classification — backfill từ `is_public` cũ) → `scripts/migrate_org_and_channels.py` (tạo bảng `units`; users: `unit_id` FK, `directive_channel_access`, `command_channel_access`, `is_system`, `must_change_password`). Tất cả idempotent. Bảng mới dùng `create_all` bình thường. Seed admin: tự chạy lúc khởi động (`app/core/bootstrap.py`) hoặc `scripts/seed_admin.py`.
- **Cơ cấu đơn vị chuẩn** (`app/core/bootstrap.py` `STANDARD_UNITS` / `ensure_standard_units`): Ban chỉ huy Lữ đoàn, Cấp uỷ – Đảng bộ Lữ đoàn, Phòng Tham mưu, Phòng Chính trị, Phòng Hậu cần – Kỹ thuật, Tiểu đoàn 1, Tiểu đoàn 2, Đại đội 5, Trạm bảo đảm, Trung tâm 2. Tự seed lúc khởi động **chỉ khi bảng `units` rỗng** (không hồi sinh đơn vị đã xoá). Bổ sung cho DB cũ: chạy tay `scripts/seed_units.py` (idempotent, so khớp theo tên). Các đầu mối này để admin gán tài khoản (`PATCH /users/{id}/unit`) và để BCH giao nhiệm vụ / nhận báo cáo theo đơn vị.
- **Đơn vị & kênh hạn chế**: bảng `units` (`unit_kind`: `phong_ban|tieu_doan|dai_doi|tram|bch_lu_doan|cap_uy`), `User.unit_id` (admin gán qua `PATCH /users/{id}/unit`).
  - **Kênh Chỉ đạo–Báo cáo** (BCH ↔ đơn vị): cấp bằng cờ `User.directive_channel_access` (admin bật/tắt qua `PATCH /users/{id}/channel-access`); `commander`/`admin` luôn có. Helper `can_access_directive_channel`, `directive_thread_scope`.
  - **Kênh chuyên BCH & Cấp uỷ** (Phase 4): gác bằng `has_secret_clearance` (`commander`/`admin` HOẶC `User.clearance = True`) — **KHÔNG** dùng `command_channel_access` nữa (cột giữ lại cho tương thích). Helper `can_access_command_channel`.
  - JWT claim `dca`/`cca`/`unit`/`mcp`/`adm` + `clr`; FE `AuthContext`: `canDirectiveChannel` (từ `dca`), `canCommandChannel` = `hasClearance || isCommander`, `unitId`/`mustChangePassword`/`isAdmin`. Chỉ là UX, enforce ở service.
- Cần `pip install python-multipart` (đã trong `requirements.txt`) cho upload file. `GET /documents/{id}/download` trả `FileResponse` (tôn trọng `classification`).
- Áp dependency ở mức router (`APIRouter(dependencies=[...])`) khi áp cho toàn bộ endpoint trong router; áp ở mức từng route (`@router.post(..., dependencies=[...])`) khi chỉ một số method cần role riêng.
- Quy tắc "chỉ tác giả hoặc `commander`" cho sửa/xoá: so sánh `current_user.role != "commander" and <owner_id> != current_user.id` → 403.
- JWT payload gồm `sub` (username), `role`, `uid` (id), `clr` (clearance bool) — set qua `create_access_token(subject, extra_claims={"role":..., "uid":..., "clr":...})` trong `POST /users/login`. Frontend đọc ở `AuthContext` (`role`, `userId`, `hasClearance`) để ẩn/hiện nút — **không thay cho enforce ở backend**, chỉ là UX.

### Quy ước khi thêm module nội dung mới (Chỉ thị-Nhiệm vụ, Hồ sơ cá nhân, ...)
Theo đúng mẫu `posts`/`education_materials` — bắt buộc đủ 3 tầng, không viết logic/query thẳng trong route:
1. `repositories/<entity>_repository.py`: hàm thuần truy vấn DB (`create`, `list_all`, `get`, `update`, `delete`), nhận `db: Session`, không import `HTTPException`.
2. `services/<entity>_service.py`: nghiệp vụ — gọi repository, raise `HTTPException` (404/403/409...), áp rule sở hữu ("chỉ tác giả hoặc `commander`"), tự join `author.full_name` thành `author_full_name` khi trả entity có tác giả (không gọi `GET /users/{id}` vì endpoint đó bị giới hạn quyền xem).
3. `api/routes/<entity>s.py`: chỉ khai báo path/method/status/`Depends`, gọi đúng 1 hàm service rồi trả kết quả — không chứa `if`/query DB.

Model kế thừa `Base`, có `author_id` (FK `users.id`) + `created_at` nếu là nội dung do người dùng đăng.

### Hợp đồng API dùng chung với Frontend
File `openapi.yaml` ở thư mục gốc dự án là bản xuất trực tiếp từ backend đang chạy (`backend/scripts/export_openapi.py`). Sau mỗi lần thêm/sửa endpoint, chạy lại script này để đồng bộ trước khi báo hoàn thành tính năng.